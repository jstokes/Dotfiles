export PATH="/sbin":$PATH
export PATH="/usr/sbin":$PATH
export PATH="/bin":$PATH
export PATH="/usr/bin":$PATH
export PATH="/usr/local/bin":$PATH
export PATH="$HOME/Developer/bin":$PATH
export PATH="/usr/local/git/bin":$PATH
export PATH="/usr/X11/bin":$PATH
export PATH="/usr/local/share/npm/bin":$PATH
export PATH="/usr/local/mysql/bin":$PATH
export PATH="/usr/local/share/npm/lib/node_modules":$PATH
export PATH="/usr/local/opt/ruby/bin":$PATH
if [ -x /opt/homebrew/opt/ruby/bin/gem ]; then
  export PATH="$(/opt/homebrew/opt/ruby/bin/gem environment gemdir)/bin":$PATH
fi
[ -d "$HOME/Library/Python/3.12/bin" ] && export PATH="$HOME/Library/Python/3.12/bin":$PATH
[ -d "/snap/bin" ] && export PATH="/snap/bin":$PATH
export TERM="screen-256color"
export LEIN_SNAPSHOTS_IN_RELEASE="true"
export EDITOR="nvim"
export VISUAL="less"
export LESS="-F -g -i -M -r -+S -w -X -z-4"

if command -v brew >/dev/null 2>&1 && brew --prefix maven >/dev/null 2>&1; then
  export M2_HOME="$(brew --prefix maven)/libexec"
fi

export JAVA_OPTS="$JAVA_OPTS -XX:-OmitStackTraceInFastThrow -XX:+UnlockDiagnosticVMOptions -XX:+DebugNonSafepoints"

HISTCONTROL=ignoreboth
HISTIGNORE='ls:bg:fg:history'
PROMPT_COMMAND='history -a'

hash -d -- play="$HOME/play"
hash -d -- work="$HOME/work"
